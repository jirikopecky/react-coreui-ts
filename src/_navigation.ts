import { Nav } from './model/navigation';

const nav: Nav = {
  items: [
    ({
      name: 'Dashboard',
      url: '/dashboard',
      icon: 'icon-speedometer',
      // badge: {
      //   variant: 'info',
      //   text: 'NEW'
      // }
    })
  ]
};

export default nav;
